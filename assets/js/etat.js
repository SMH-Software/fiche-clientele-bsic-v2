/*const btnAddFormEtat = document.querySelector('#addEtat')

const formEtat = document.querySelector('#form-etat')


btnAddFormEtat.addEventListener('click', (e) => {
    btnAddFormEtat.style.display = 'none'
    formEtat.style.display = 'flex'
})

const trow = document.querySelectorAll('#trow')
const popup = document.querySelector('.popup')

trow.forEach((row) => {
    row.addEventListener('mouseover', () => {
        popup.style.opacity = 1
        popup.innerHTML = `
            <strong>Detail de l'événement </strong>
            <p>${row.getAttribute('data-details')}</p>
        `
    })

    row.addEventListener('mouseout', () => {
        popup.style.opacity = 0
    })
})*/

